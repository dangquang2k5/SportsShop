server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/SportsStoreDB
    username: root
    password: 030705
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: sports-secret-key-2024

logging:
  level:
    com.sportsshop: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE